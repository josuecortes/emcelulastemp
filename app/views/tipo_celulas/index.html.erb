<p id="notice"><%= notice %></p>

<h1>Listing Tipo_celulas</h1>

<table id="tipo_celulas" class="display table table-hover" cellspacing="0" width="100%">
  <thead>
    <tr>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_celulas.each do |tipo_celula| %>
      <tr>
        <tr></tr>
        <td><%= link_to 'Show', tipo_celula %>
        <%= link_to 'Edit', edit_tipo_celula_path(tipo_celula) %></td>
        <%= link_to 'Destroy', tipo_celula, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo celula', new_tipo_celula_path %>
